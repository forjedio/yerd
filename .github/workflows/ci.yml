name: CI

on:
  pull_request:
  push:
    branches: [main]

# One run per ref; newer pushes cancel older in-progress CI.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  # The exact local gate (CLAUDE.md §5): fmt + clippy -D warnings + test, over
  # the full workspace (which includes the Tauri GUI crate) on Linux + macOS.
  rust:
    name: rust (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # macos-14 = Apple Silicon, macos-13 = Intel — both, so an x86_64-only
        # macOS break is caught in CI before a release tag is pushed.
        os: [ubuntu-22.04, macos-13, macos-14]
    steps:
      - uses: actions/checkout@v4

      # The GUI crate links GTK/WebKit/etc., so `clippy/test --workspace` needs
      # the -dev packages on Linux. macOS uses system frameworks (nothing extra).
      - name: Install Linux GUI build deps
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libwebkit2gtk-4.1-dev libgtk-3-dev libsoup-3.0-dev \
            libjavascriptcoregtk-4.1-dev libayatana-appindicator3-dev \
            libdbus-1-dev libxdo-dev librsvg2-dev

      # Toolchain comes from rust-toolchain.toml (1.96.0 + clippy + rustfmt);
      # rustup installs it on first use.
      - name: Show toolchain
        run: rustup show active-toolchain

      - uses: Swatinem/rust-cache@v2

      - name: Format
        if: runner.os == 'Linux'
        run: cargo fmt --all --check

      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Test
        run: cargo test --workspace

  # Frontend unit/component tests + production build (type-check via vue-tsc).
  frontend:
    name: frontend
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: apps/yerd-gui
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/yerd-gui/package-lock.json

      - name: Install deps
        run: npm ci

      - name: Test
        run: npm run test

      - name: Build
        run: npm run build
