{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core access plus the opener/dialog plugin commands the host helpers use. App commands (our #[tauri::command]s) are permitted by registration.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default", "dialog:default"]
}
